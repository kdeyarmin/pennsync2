{
  "name": "PatientOutcomeMetric",
  "type": "object",
  "properties": {
    "patient_id": {
      "type": "string"
    },
    "episode_start": {
      "type": "string",
      "format": "date"
    },
    "episode_end": {
      "type": "string",
      "format": "date"
    },
    "admission_source": {
      "type": "string",
      "enum": [
        "hospital",
        "community",
        "snf",
        "other"
      ]
    },
    "discharge_disposition": {
      "type": "string",
      "enum": [
        "remained_home",
        "remained_community_with_hha",
        "non_institutional_hospice",
        "moved_out_of_service_area",
        "hospital",
        "snf",
        "deceased",
        "other"
      ],
      "description": "Discharge disposition. The community and hospice values come from OASIS-E2 M2420 codes 1-4. 'hospital' and 'snf' are NOT derivable from M2420 — under the current instrument its codes 2-4 name community and hospice destinations, not facilities — so they can only come from M2410 (Inpatient Facility), which PennSync does not implement. They remain in the enum because rows written before this correction carry them."
    },
    "length_of_service": {
      "type": "number",
      "description": "Days in service"
    },
    "total_visits_provided": {
      "type": "number"
    },
    "readmission_30_day": {
      "type": "boolean"
    },
    "readmission_60_day": {
      "type": "boolean"
    },
    "er_visit_30_day": {
      "type": "boolean"
    },
    "functional_improvement": {
      "type": "object",
      "properties": {
        "ambulation_improved": {
          "type": "boolean"
        },
        "bathing_improved": {
          "type": "boolean"
        },
        "transferring_improved": {
          "type": "boolean"
        },
        "medication_management_improved": {
          "type": "boolean"
        },
        "dyspnea_improved": {
          "type": "boolean"
        },
        "overall_improvement_score": {
          "type": "number"
        }
      }
    },
    "gg_discharge_function_score": {
      "type": "number",
      "description": "Raw GG Discharge Function Score (GG0130 self-care + GG0170 mobility) when GG items are present"
    },
    "measure_results": {
      "type": "array",
      "description": "Per-measure CMS outcome results computed by the outcome-measure engine",
      "items": {
        "type": "object",
        "properties": {
          "measure": { "type": "string" },
          "status": {
            "type": "string",
            "enum": ["improved", "not_improved", "excluded"]
          },
          "start_value": { "type": "number" },
          "discharge_value": { "type": "number" },
          "reason": { "type": "string" }
        }
      }
    },
    "input_response_schema_ids": {
      "type": "array",
      "items": { "type": "string" },
      "description": "The response schema id of each episode endpoint (SOC/ROC, Discharge). A record without both endpoints on 'pennsync-oasis-response-v2-cms-e2' was computed from response meanings PennSync cannot vouch for and must be excluded from current CMS-labeled aggregates."
    },
    "source_assessment_ids": {
      "type": "array",
      "items": { "type": "string" },
      "description": "The OASISAssessment ids this metric was computed from, so a reviewer can re-derive it."
    },
    "instrument_versions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "CMS instrument version at each endpoint."
    },
    "calculation_version": {
      "type": "string",
      "description": "Version of the deterministic outcome core that produced this record. Bumped when the rules change meaning, so stale records can be retired rather than silently re-read under new rules."
    },
    "retired_reason": {
      "type": "string",
      "description": "Why this metric was retired from current CMS-labeled views — the episode's exclusion reasons at the time it was retired. The row is KEPT so the history stays auditable; it is not deleted."
    },
    "retired_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the metric was retired."
    },
    "outcome_measure_source": {
      "type": "string",
      "description": "Provenance of the outcome metric. 'oasis_change_score' is a live metric; 'retired_unverified_schema' means the episode it came from could not be verified under the CMS-aligned response schema, so the row must be excluded from current CMS-labeled aggregates while remaining auditable."
    },
    "pph_prevention": {
      "type": "object",
      "description": "Within-Stay Potentially Preventable Hospitalization prevention: captured risk, interventions delivered, and the outcome (from the PPH worklist engine)",
      "properties": {
        "risk_score": { "type": "number" },
        "risk_level": { "type": "string" },
        "within_dtc_pac_window": { "type": "boolean" },
        "interventions": { "type": "array", "items": { "type": "string" } },
        "rehospitalized": { "type": "boolean" },
        "measure": { "type": "string" }
      }
    },
    "care_plan_goals_achieved": {
      "type": "number",
      "description": "Number of goals met"
    },
    "care_plan_goals_total": {
      "type": "number"
    },
    "goal_achievement_rate": {
      "type": "number",
      "description": "Percentage"
    },
    "wound_healing_outcome": {
      "type": "string",
      "enum": [
        "healed",
        "improved",
        "stable",
        "deteriorated",
        "n/a"
      ]
    },
    "pain_improvement": {
      "type": "number",
      "description": "Pain level change (admission to discharge)"
    },
    "patient_satisfaction_score": {
      "type": "number",
      "description": "1-5 rating"
    },
    "complications": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string"
          },
          "severity": {
            "type": "string"
          },
          "date": {
            "type": "string",
            "format": "date"
          }
        }
      }
    },
    "adverse_events": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "outcome_quality_score": {
      "type": "number",
      "description": "Composite quality score 0-100"
    },
    "primary_diagnosis": {
      "type": "string"
    },
    "comorbidities_count": {
      "type": "number"
    }
  },
  "required": [
    "patient_id",
    "episode_start"
  ]
}