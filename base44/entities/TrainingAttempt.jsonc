{
  "name": "TrainingAttempt",
  "type": "object",
  "properties": {
    "assignment_id": {
      "type": "string",
      "description": "Reference to TrainingAssignment"
    },
    "course_id": {
      "type": "string",
      "description": "Reference to TrainingCourse"
    },
    "user_id": {
      "type": "string",
      "description": "User taking the course"
    },
    "started_at": {
      "type": "string",
      "format": "date-time",
      "description": "When attempt started"
    },
    "submitted_at": {
      "type": "string",
      "format": "date-time",
      "description": "When attempt was submitted"
    },
    "score": {
      "type": "number",
      "description": "Score percentage"
    },
    "objective_score": {
      "type": "number",
      "description": "Auto-graded score for objective questions"
    },
    "short_answer_score": {
      "type": "number",
      "description": "Score contribution from short answer or scenario responses"
    },
    "passed": {
      "type": "boolean",
      "description": "Whether attempt passed"
    },
    "pass_fail_result": {
      "type": "string",
      "enum": [
        "passed",
        "failed"
      ],
      "description": "Pass/fail result for the attempt"
    },
    "answers_json": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "description": "User answers"
    },
    "grading_method": {
      "type": "string",
      "enum": [
        "automatic",
        "ai_assisted",
        "manual_review"
      ],
      "default": "automatic",
      "description": "How the attempt was graded"
    },
    "ai_grading_confidence": {
      "type": "number",
      "description": "Confidence score for AI-assisted grading"
    },
    "randomized_question_order": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Question order shown to the learner"
    },
    "time_spent_minutes": {
      "type": "number",
      "description": "Time spent in minutes"
    },
    "attempt_number": {
      "type": "number",
      "default": 1,
      "description": "Attempt number"
    },
    "remediation_message": {
      "type": "string",
      "description": "Remediation guidance for failed attempt"
    },
    "ip_address": {
      "type": "string",
      "description": "IP address for audit"
    },
    "badges_processed_at": {
      "type": "string",
      "format": "date-time",
      "description": "Set by awardBadgeOnCompletion once this attempt has been processed; gates idempotent badge/point/streak awards so a replayed attempt cannot double-award (covers attempts that earn no badge too)."
    },
    "badges_claim_token": {
      "type": "string",
      "description": "Best-effort claim token written with badges_processed_at; awardBadgeOnCompletion re-reads and proceeds only if this token still matches. Not true CAS — Base44 has no conditional update / If-Match."
    },
    "corrective_plan_claimed_by": {
      "type": "string",
      "description": "Best-effort claim token for triggerCorrectiveActionPlan so concurrent trigger fires do not create duplicate plans"
    }
  },
  "required": [
    "course_id",
    "user_id",
    "started_at"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "user_id": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    // Write is admin-only ON PURPOSE: attempts are graded and written
    // server-side (gradeTrainingAttempt, via the service role), and a passing
    // TrainingAttempt row is the evidence issueCertificate trusts. If users
    // could write their own attempts they could mint certificates.
    "write": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}